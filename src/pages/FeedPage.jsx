import React from 'react';
import RecentsPage from './RecentsPage';

// Keep the legacy /feed entry point aligned with the Recently Played page.
const FeedPage = () => <RecentsPage />;

export default FeedPage;
